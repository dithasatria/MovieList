<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group
        android:id="@+id/menu_top"
        android:checkableBehavior="single">
        <item
            android:id="@+id/nav_discover"
            android:icon="@drawable/ic_movie_discover"
            android:title="Discover" />
        <item
            android:id="@+id/nav_search"
            android:icon="@drawable/ic_movie_search"
            android:title="Search" />
        <item
            android:id="@+id/nav_rated"
            android:icon="@drawable/ic_movie_star"
            android:title="Favorites" />
    </group>

    <group
        android:id="@+id/menu_bottom"
        android:checkableBehavior="single">
        <item
            android:id="@+id/nav_settings"
            android:icon="@drawable/ic_movie_settings"
            android:title="Settings" />
        <item
            android:id="@+id/nav_about"
            android:icon="@drawable/ic_movie_help"
            android:title="About" />

    </group>

</menu>
